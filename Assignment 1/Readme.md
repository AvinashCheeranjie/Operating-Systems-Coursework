# Contributions

## Avinash Cheeranjie:
  * `proc_read()`
  * `proc_init()`

## Habel Kingson:
  * Debugging
       - Tested output with loading, unloading and invoking `cat /proc/seconds` on the `seconds.ko` file to verify correct output in terminal.
  * `proc_exit()`
